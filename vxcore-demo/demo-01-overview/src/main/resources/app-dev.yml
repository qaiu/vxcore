# 服务配置
server:
  port: 18081
  contextPath: /

# vertx核心线程配置
vertx:
  eventLoopPoolSize: 0
  workerPoolSize: 0

# vertx-service配置
custom:
  asyncServiceInstances: 4
  baseLocations: cn.qaiu.demo.overview
  routeTimeOut: 15000
  gatewayPrefix: /
