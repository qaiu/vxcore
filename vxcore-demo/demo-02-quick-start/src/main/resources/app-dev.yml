# 服务配置
server:
  port: 18082
  contextPath: /
  enableDatabase: false

# vertx核心线程配置
vertx:
  eventLoopPoolSize: 0
  workerPoolSize: 0

# vertx-service配置
custom:
  asyncServiceInstances: 4
  baseLocations: cn.qaiu.demo.quickstart
  routeTimeOut: 15000
  gatewayPrefix: /
