server:
  port: 0  # 使用随机端口避免测试时端口冲突

datasources:
  default:
    url: "jdbc:h2:mem:testdb"
    driver: "org.h2.Driver"
    username: "sa"
    password: ""

custom:
  baseLocations: "cn.qaiu.example"
